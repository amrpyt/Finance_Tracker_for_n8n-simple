{
  "name": "finance-tracker-telegram-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:bot": "cd bot && npm run dev",
    "dev:convex": "cd convex && npm run dev",
    "test": "cd bot && npm test && cd ../convex && npm test",
    "lint": "cd bot && npm run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.1.0"
  }
}
